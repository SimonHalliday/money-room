[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: send everything to index.html (the magic-link redirect lands here too)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
